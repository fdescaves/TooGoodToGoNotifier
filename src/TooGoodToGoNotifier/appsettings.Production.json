{
  "NLog": {
    "rules": [
      {
        "enabled": true,
        "logger": "*",
        "minLevel": "Info",
        "writeTo": "file-log"
      },
      {
        "enabled": true,
        "logger": "*",
        "minLevel": "Warning",
        "writeTo": "email-log"
      }
    ]
  }
}
